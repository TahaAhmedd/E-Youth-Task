const LoadingScreen = () => {
  return (
    <section className='d-flex h-screen align-items-center justify-content-center'>
      <span className='loader' />
    </section>
  );
};

export default LoadingScreen;
